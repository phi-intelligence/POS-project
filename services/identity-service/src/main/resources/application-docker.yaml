server:
  port: 8081

spring:
  config:
    activate:
      on-profile: docker

  application:
    name: identity-service

  datasource:
    url: jdbc:postgresql://${DB_HOST:db}:${DB_PORT:5432}/${DB_NAME:pos}
    username: ${DB_USERNAME:pos}
    password: ${DB_PASSWORD:pos}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        default_schema: identity

  flyway:
    enabled: true
    schemas: identity
    locations: classpath:db/migration

jwt:
  secret: ${JWT_SECRET:change-me-identity-jwt-secret}
  access-token-seconds: 900
  refresh-token-seconds: 1209600

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
